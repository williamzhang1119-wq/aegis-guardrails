# Optional. Without a key the app uses the offline fallback responder.
# The same guardrail pipeline wraps whichever provider is selected.

# OPENAI_API_KEY=
# OPENAI_MODEL=gpt-4o-mini

# ANTHROPIC_API_KEY=
# ANTHROPIC_MODEL=claude-3-5-haiku-latest

# Optional. If set, GET /api/checkup requires x-checkup-token or Bearer.
# CHECKUP_TOKEN=
